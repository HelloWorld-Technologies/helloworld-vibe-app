# Copy to keystore.properties and fill in real values.
# This file is safe to commit; keystore.properties is not.

storeFile=hello-world-key.keystore
keyAlias=HelloWorld
storePassword=YOUR_STORE_PASSWORD
password=YOUR_KEY_PASSWORD
